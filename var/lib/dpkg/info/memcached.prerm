#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/memcached" ] || [ -e "/etc/init/memcached.conf" ]; then
	invoke-rc.d memcached stop || exit $?
fi
# End automatically added section
